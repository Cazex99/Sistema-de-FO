Já dentro da pasta do sistema, rode no terminal os seguintes comandos 


sudo nano /etc/systemd/system/avaliacao.service

[Unit]
Description=Sistema Avaliacao Militar
After=network.target

[Service]
User=avaliacao
WorkingDirectory=/home/avaliacao/avaliacao_militar

Environment="PYTHONUNBUFFERED=1"
Environment="DATABASE_URL=postgresql://avaliacao_user:*******@localhost/avaliacao"
Environment="PATH=/home/avaliacao/.local/bin:/usr/bin:/bin"

ExecStart=/home/avaliacao/.local/bin/gunicorn \
--chdir /home/avaliacao/avaliacao_militar \
--bind 127.0.0.1:8000 \
--workers 2 \
--threads 4 \
--worker-class gthread \
--timeout 60 \
--keep-alive 10 \
--max-requests 500 \
--max-requests-jitter 100 \
--preload \
app:app

Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target

salva e fecha ( ctrl+o depois ctrl+x )

nano /etc/nginx/sites-available/avaliacao

server {
    listen 80;
    server_name _;

    location / {
        proxy_pass http://127.0.0.1:8000;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;

salva e fecha 
